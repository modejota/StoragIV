import { FastifyInstance } from "fastify";

export default async function facturaController(fastify:FastifyInstance) {
    
    fastify.get('/',async function (request, reply) {
        
    })

    fastify.get('/size',async function (request, reply) {
        
    })

    fastify.post('/',async function (request, reply) {
        
    })

    fastify.get('/:id',async function (request, reply) {
        
    })

    fastify.delete('/:id',async function (request, reply) {
        
    })

    fastify.post('/:id',async function (request, reply) {
        
    })

    fastify.get('/:id/product/:idp',async function (request, reply) {
        
    })

    fastify.delete('/:id/product/:idp',async function (request, reply) {
        
    })

    fastify.put('/:id/product/:idp',async function (request, reply) {
        
    })

    fastify.get('/:id/total',async function (request, reply) {
        
    })

    fastify.get('/:id/size',async function (request, reply) {
        
    })
}